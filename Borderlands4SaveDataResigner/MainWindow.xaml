<Window x:Class="Borderlands4SaveDataResigner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Borderlands 4 Save Data Resigner" 
        Height="750" 
        Width="950"
        MinHeight="650"
        MinWidth="850"
        Background="#FF1A1A1A"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Define clean, professional colors -->
        <SolidColorBrush x:Key="PrimaryBackground" Color="#FF1A1A1A"/>
        <SolidColorBrush x:Key="SecondaryBackground" Color="#FF2A2A2A"/>
        <SolidColorBrush x:Key="AccentColor" Color="#FF0078D4"/>
        <SolidColorBrush x:Key="AccentHover" Color="#FF106EBE"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#FFB3B3B3"/>
        <SolidColorBrush x:Key="BorderColor" Color="#FF404040"/>
        <SolidColorBrush x:Key="InputBackground" Color="#FF333333"/>
        <SolidColorBrush x:Key="HoverBackground" Color="#FF3A3A3A"/>
        <SolidColorBrush x:Key="SuccessColor" Color="#FF107C10"/>
        <SolidColorBrush x:Key="WarningColor" Color="#FFFF9500"/>
        <SolidColorBrush x:Key="ErrorColor" Color="#FFD13438"/>

        <!-- Professional Tab Style -->
        <Style x:Key="ModernTabItem" TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border" 
                                Background="{StaticResource SecondaryBackground}" 
                                BorderBrush="{StaticResource BorderColor}" 
                                BorderThickness="1,1,1,0" 
                                CornerRadius="6,6,0,0" 
                                Margin="2,0,0,0">
                            <ContentPresenter x:Name="ContentSite"
                                              ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="16,12"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentColor}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                                <Setter Property="FontWeight" Value="Normal"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource HoverBackground}"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsSelected" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentHover}"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF0E5A9E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{StaticResource SecondaryBackground}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{StaticResource BorderColor}"
                                BorderThickness="1"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource HoverBackground}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource BorderColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource InputBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Focusable="false" 
                                          HorizontalScrollBarVisibility="Hidden" 
                                          VerticalScrollBarVisibility="Hidden"
                                          Margin="2"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                                <Setter TargetName="border" Property="Background" Value="#FF3A3A3A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern GroupBox Style -->
        <Style x:Key="ModernGroupBox" TargetType="GroupBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,10"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="1" 
                                    Background="{StaticResource SecondaryBackground}"
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6">
                                <ContentPresenter Grid.Row="1" 
                                                  Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <Label Grid.Row="0" 
                                   Content="{TemplateBinding Header}" 
                                   Foreground="{StaticResource AccentColor}"
                                   FontWeight="SemiBold"
                                   FontSize="15"
                                   Margin="10,0,0,5"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern ProgressBar Style -->
        <Style x:Key="ModernProgressBar" TargetType="ProgressBar">
            <Setter Property="Background" Value="{StaticResource InputBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4">
                            <Grid x:Name="PART_Track">
                                <Border x:Name="PART_Indicator" 
                                        Background="{TemplateBinding Foreground}" 
                                        HorizontalAlignment="Left"
                                        CornerRadius="4"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*" MinHeight="250"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,16">
            <TextBlock Text="Borderlands 4 Save Data Resigner" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="{StaticResource AccentColor}" 
                       HorizontalAlignment="Center"/>
            <TextBlock Text="Tool for decrypting, encrypting, and resigning your save files" 
                       FontSize="14" 
                       Foreground="{StaticResource TextSecondary}" 
                       HorizontalAlignment="Center"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Main Content Tabs -->
        <TabControl Grid.Row="1" 
                    Background="{StaticResource SecondaryBackground}" 
                    BorderBrush="{StaticResource BorderColor}"
                    BorderThickness="1">
            <TabControl.Resources>
                <Style TargetType="TabItem" BasedOn="{StaticResource ModernTabItem}"/>
            </TabControl.Resources>

            <!-- Decrypt Tab -->
            <TabItem Header="🔓 Decrypt">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Convert encrypted .sav files to readable .yaml format" 
                                   FontSize="14" 
                                   Foreground="{StaticResource TextSecondary}" 
                                   Margin="0,0,0,12"/>

                        <GroupBox Header="Configuration" 
                                  Style="{StaticResource ModernGroupBox}"
                                  Margin="0,0,0,12">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Label Grid.Row="0" Grid.Column="0" 
                                       Content="Input Folder:" 
                                       Foreground="{StaticResource TextPrimary}" 
                                       VerticalAlignment="Center"
                                       FontWeight="Medium"/>
                                <TextBox x:Name="DecryptInputPath" 
                                         Grid.Row="0" Grid.Column="1" 
                                         Margin="8,4" 
                                         Style="{StaticResource ModernTextBox}"
                                         IsReadOnly="True"
                                         ToolTip="Select folder containing .sav files"/>
                                <Button Grid.Row="0" Grid.Column="2" 
                                        Content="Browse" 
                                        Width="80" 
                                        Margin="4" 
                                        Style="{StaticResource SecondaryButton}"
                                        Click="DecryptBrowseInputButton_Click"/>

                                <Label Grid.Row="1" Grid.Column="0" 
                                       Content="User ID:" 
                                       Foreground="{StaticResource TextPrimary}" 
                                       VerticalAlignment="Center"
                                       FontWeight="Medium"/>
                                <TextBox x:Name="DecryptUserId" 
                                         Grid.Row="1" Grid.Column="1" 
                                         Margin="8,4" 
                                         Style="{StaticResource ModernTextBox}"
                                         ToolTip="Enter the Steam ID or Epic ID of the save file owner"/>
                            </Grid>
                        </GroupBox>

                        <Button Content="🚀 Start Decryption" 
                                Click="DecryptButton_Click" 
                                HorizontalAlignment="Center"
                                Style="{StaticResource ModernButton}"
                                Padding="24,10" 
                                FontSize="14" 
                                FontWeight="SemiBold"
                                Margin="0,8,0,12"/>

                        <GroupBox Header="Progress" 
                                  Style="{StaticResource ModernGroupBox}">
                            <StackPanel>
                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <ProgressBar x:Name="DecryptProgressBar" 
                                                 Grid.Column="0"
                                                 Style="{StaticResource ModernProgressBar}"
                                                 Margin="0,0,8,0"/>
                                    <TextBlock Grid.Column="1" 
                                               x:Name="DecryptProgressText" 
                                               Text="0%" 
                                               Foreground="{StaticResource AccentColor}" 
                                               VerticalAlignment="Center"
                                               FontWeight="Medium"
                                               MinWidth="35"/>
                                </Grid>
                                <TextBlock x:Name="DecryptStatusText" 
                                           Text="Ready to decrypt files" 
                                           Foreground="{StaticResource TextSecondary}"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Encrypt Tab -->
            <TabItem Header="🔒 Encrypt">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Convert .yaml files back to encrypted .sav format" 
                                   FontSize="14" 
                                   Foreground="{StaticResource TextSecondary}" 
                                   Margin="0,0,0,12"/>

                        <GroupBox Header="Configuration" 
                                  Style="{StaticResource ModernGroupBox}"
                                  Margin="0,0,0,12">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Label Grid.Row="0" Grid.Column="0" 
                                       Content="Input Folder:" 
                                       Foreground="{StaticResource TextPrimary}" 
                                       VerticalAlignment="Center"
                                       FontWeight="Medium"/>
                                <TextBox x:Name="EncryptInputPath" 
                                         Grid.Row="0" Grid.Column="1" 
                                         Margin="8,4" 
                                         Style="{StaticResource ModernTextBox}"
                                         IsReadOnly="True"
                                         ToolTip="Select folder containing .yaml files"/>
                                <Button Grid.Row="0" Grid.Column="2" 
                                        Content="Browse" 
                                        Width="80" 
                                        Margin="4" 
                                        Style="{StaticResource SecondaryButton}"
                                        Click="EncryptBrowseInputButton_Click"/>

                                <Label Grid.Row="1" Grid.Column="0" 
                                       Content="User ID:" 
                                       Foreground="{StaticResource TextPrimary}" 
                                       VerticalAlignment="Center"
                                       FontWeight="Medium"/>
                                <TextBox x:Name="EncryptUserId" 
                                         Grid.Row="1" Grid.Column="1" 
                                         Margin="8,4" 
                                         Style="{StaticResource ModernTextBox}"
                                         ToolTip="Enter the Steam ID or Epic ID for the encrypted save files"/>
                            </Grid>
                        </GroupBox>

                        <Button Content="🔐 Start Encryption" 
                                Click="EncryptButton_Click" 
                                HorizontalAlignment="Center"
                                Style="{StaticResource ModernButton}"
                                Padding="24,10" 
                                FontSize="14" 
                                FontWeight="SemiBold"
                                Margin="0,8,0,12"/>

                        <GroupBox Header="Progress" 
                                  Style="{StaticResource ModernGroupBox}">
                            <StackPanel>
                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <ProgressBar x:Name="EncryptProgressBar" 
                                                 Grid.Column="0"
                                                 Style="{StaticResource ModernProgressBar}"
                                                 Margin="0,0,8,0"/>
                                    <TextBlock Grid.Column="1" 
                                               x:Name="EncryptProgressText" 
                                               Text="0%" 
                                               Foreground="{StaticResource AccentColor}" 
                                               VerticalAlignment="Center"
                                               FontWeight="Medium"
                                               MinWidth="35"/>
                                </Grid>
                                <TextBlock x:Name="EncryptStatusText" 
                                           Text="Ready to encrypt files" 
                                           Foreground="{StaticResource TextSecondary}"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Resign Tab -->
            <TabItem Header="🔄 Resign">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Transfer save files from one user account to another" 
                                   FontSize="14" 
                                   Foreground="{StaticResource TextSecondary}" 
                                   Margin="0,0,0,12"/>

                        <GroupBox Header="Configuration" 
                                  Style="{StaticResource ModernGroupBox}"
                                  Margin="0,0,0,12">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Label Grid.Row="0" Grid.Column="0" 
                                       Content="Input Folder:" 
                                       Foreground="{StaticResource TextPrimary}" 
                                       VerticalAlignment="Center"
                                       FontWeight="Medium"/>
                                <TextBox x:Name="ResignInputPath" 
                                         Grid.Row="0" Grid.Column="1" 
                                         Margin="8,4" 
                                         Style="{StaticResource ModernTextBox}"
                                         IsReadOnly="True"
                                         ToolTip="Select folder containing .sav files"/>
                                <Button Grid.Row="0" Grid.Column="2" 
                                        Content="Browse" 
                                        Width="80" 
                                        Margin="4" 
                                        Style="{StaticResource SecondaryButton}"
                                        Click="ResignBrowseInputButton_Click"/>

                                <Label Grid.Row="1" Grid.Column="0" 
                                       Content="Source User ID:" 
                                       Foreground="{StaticResource TextPrimary}" 
                                       VerticalAlignment="Center"
                                       FontWeight="Medium"/>
                                <TextBox x:Name="ResignInputUserId" 
                                         Grid.Row="1" Grid.Column="1" 
                                         Margin="8,4" 
                                         Style="{StaticResource ModernTextBox}"
                                         ToolTip="Enter the Steam ID or Epic ID of the current save file owner"/>

                                <Label Grid.Row="2" Grid.Column="0" 
                                       Content="Target User ID:" 
                                       Foreground="{StaticResource TextPrimary}" 
                                       VerticalAlignment="Center"
                                       FontWeight="Medium"/>
                                <TextBox x:Name="ResignOutputUserId" 
                                         Grid.Row="2" Grid.Column="1" 
                                         Margin="8,4" 
                                         Style="{StaticResource ModernTextBox}"
                                         ToolTip="Enter the Steam ID or Epic ID for the new save file owner"/>
                            </Grid>
                        </GroupBox>

                        <Button Content="🔄 Start Resigning" 
                                Click="ResignButton_Click" 
                                HorizontalAlignment="Center"
                                Style="{StaticResource ModernButton}"
                                Padding="24,10" 
                                FontSize="14" 
                                FontWeight="SemiBold"
                                Margin="0,8,0,12"/>

                        <GroupBox Header="Progress" 
                                  Style="{StaticResource ModernGroupBox}">
                            <StackPanel>
                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <ProgressBar x:Name="ResignProgressBar" 
                                                 Grid.Column="0"
                                                 Style="{StaticResource ModernProgressBar}"
                                                 Margin="0,0,8,0"/>
                                    <TextBlock Grid.Column="1" 
                                               x:Name="ResignProgressText" 
                                               Text="0%" 
                                               Foreground="{StaticResource AccentColor}" 
                                               VerticalAlignment="Center"
                                               FontWeight="Medium"
                                               MinWidth="35"/>
                                </Grid>
                                <TextBlock x:Name="ResignStatusText" 
                                           Text="Ready to resign files" 
                                           Foreground="{StaticResource TextSecondary}"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <!-- Output Path Display -->
        <Border Grid.Row="2" 
                Background="{StaticResource SecondaryBackground}"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="12,8"
                Margin="0,12,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           Text="📁 Output: " 
                           Foreground="{StaticResource AccentColor}" 
                           FontWeight="Medium"
                           FontSize="13"
                           VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1"
                           x:Name="OutputPathTextBlock" 
                           Foreground="{StaticResource TextSecondary}" 
                           VerticalAlignment="Center"
                           FontSize="13"
                           Margin="6,0,0,0"
                           TextTrimming="CharacterEllipsis"/>
            </Grid>
        </Border>

        <!-- Activity Log -->
        <GroupBox Grid.Row="3" 
                  Header="📋 Activity Log" 
                  Style="{StaticResource ModernGroupBox}"
                  Margin="0,16,0,0">
            <Border Background="{StaticResource InputBackground}"
                    BorderBrush="{StaticResource BorderColor}"
                    BorderThickness="1"
                    CornerRadius="4">
                <ScrollViewer x:Name="LogScrollViewer"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Auto"
                              Padding="8">
                    <TextBlock x:Name="LogTextBlock" 
                               Foreground="{StaticResource TextPrimary}" 
                               Padding="12"
                               TextWrapping="Wrap"
                               FontFamily="Consolas"
                               FontSize="13"
                               LineHeight="18"
                               Background="Transparent"
                               Text="[18:53:25] Info: Application started&#x0a;Ready to process save files..."/>
                </ScrollViewer>
            </Border>
        </GroupBox>
    </Grid>
</Window>